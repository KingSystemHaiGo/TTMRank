{
  "name": "ttmrank-web",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/web/*.test.js tests/worker/*.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1"
  }
}
